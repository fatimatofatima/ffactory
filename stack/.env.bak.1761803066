PGPORT=5433
PGUSER=forensic_user  
PGPASSWORD=password
PGDB=forensic_db
NEO4J_AUTH=none
MINIO_PORT=9000
REDIS_PORT=6379

# منافذ بديلة لتجنب التعارض
MINIO_PORT=9002
OLLAMA_PORT=11435

# === MinIO credentials (لازم طول كلمة السر ≥ 8) ===
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=ChangeMe_12345

# Console على 9002 في الهست و 9001 داخل الحاوية (زي ما هو في compose)
MINIO_PORT=9002

# ضبط التايمزون (كان بيطلع Warning)
TZ=Asia/Kuwait

# لو حابب تغيير Ollama:
OLLAMA_PORT=11435

# === MinIO credentials (لازم طول كلمة السر ≥ 8) ===
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=ChangeMe_12345

# Console على 9002 في الهست و 9001 داخل الحاوية (زي ما هو في compose)
MINIO_PORT=9002

# ضبط التايمزون (كان بيطلع Warning)
TZ=Asia/Kuwait

# Ollama port
OLLAMA_PORT=11435
MINIO_CON=
